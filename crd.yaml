apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: guarduims.guard.example.com
spec:
  group: guard.example.com
  names:
    kind: Guarduim
    plural: guarduims
    singular: guarduim
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                username:
                  type: string
                threshold:
                  type: integer
            status:
              type: object
              properties:
                failedAttempts:
                  type: integer
                isBlocked:
                  type: boolean
